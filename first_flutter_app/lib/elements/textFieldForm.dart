// 表单

import 'package:flutter/material.dart';

class TextFieldForm extends StatelessWidget {
  @override 
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text("表单"),
      ),
      body: Center(
        child: new TextFieldFormRoute(),
      ),
    );
  }
}
class  TextFieldFormRoute  extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Center(

    );
  }
}