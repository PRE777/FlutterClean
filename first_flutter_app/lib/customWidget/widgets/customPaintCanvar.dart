import 'package:flutter/material.dart';

class CustomPaintCanvarRoute extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(" 自绘组件(CustomPaint与Canvas)",style: TextStyle(fontSize: 17),),
      ),
      body: Center(),
    );
  }
}